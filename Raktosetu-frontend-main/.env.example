# Copy to .env and fill in
# Local dev:
VITE_API_BASE_URL=http://127.0.0.1:8000
# Production (set this value as the Netlify environment variable VITE_API_BASE_URL,
# then trigger "Clear cache and deploy" — Vite injects it at build time):
# VITE_API_BASE_URL=https://raktosetu-backend.onrender.com
